{
  "devDependencies": {
    "supabase": "^2.48.3",
    "@coral-xyz/anchor": "^0.29.0",
    "@solana/web3.js": "^1.87.6",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "deploy:frontend": "cd frontend && (npm run build || true) && firebase deploy",
    "deploy:solana": "ts-node scripts/deploy.ts"
  }
}
